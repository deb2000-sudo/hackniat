# =============================================================================
# Shared defaults — loaded by Vite in ALL modes.
# Mode-specific overrides live in .env.development and .env.production.
# Create a .env.local (git-ignored) to override any value on your machine.
# =============================================================================

# Target the dev proxy forwards API requests to (see vite.config.js).
# Only used in development, but kept here as a sensible default.
VITE_API_TARGET=http://localhost:8000
# Firebase Web SDK (Phone Auth). Same project as the backend.
# Authentication → Project settings → Your apps → Web app.
VITE_FIREBASE_API_KEY=AIzaSyD_HM8OelBit2aAG9G3kz4sZY4WE1v9UIk
VITE_FIREBASE_AUTH_DOMAIN=nxt-acad-hackathon.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=nxt-acad-hackathon
VITE_FIREBASE_APP_ID=1:835728304610:web:4e012c03741c86c83971bb
